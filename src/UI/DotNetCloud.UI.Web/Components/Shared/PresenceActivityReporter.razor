@namespace DotNetCloud.UI.Web.Components.Shared

@*
    Presence activity reporter — no visible UI. Attaches global (throttled) interaction
    listeners once per interactive circuit and forwards genuine activity to the presence
    service so the signed-in user's presence dot stays green while they interact with any
    page of the web app. Code-behind: PresenceActivityReporter.razor.cs
*@
